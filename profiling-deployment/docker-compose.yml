services:
  # MongoDB Service
  mongodb:
    image: mongo:4.4.29
    container_name: profiling-mongodb
    ports:
      - "${MONGO_PORT:-57017}:27017"
    environment:
      MONGO_INITDB_DATABASE: ${MONGODB_DATABASE:-profiling_db}
    volumes:
      - mongodb_data:/data/db
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    networks:
      - profiling-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "mongo", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Backend Service (Spring Boot)
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
    image: profiling_backend
    container_name: profiling-backend
    ports:
      - "9091:9090"
    env_file:
      - .env
    environment:
      MONGODB_URI: ${MONGODB_URI:-mongodb://profiling-mongodb:27017/profiling_db}
      MONGODB_DATABASE: ${MONGODB_DATABASE:-profiling_db}
      FRONTEND_URL: ${FRONTEND_URL:-http://saarthix.com:8090/i5h0t1r1a2a2s.com/profiling/}
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}
      JWT_SECRET: ${JWT_SECRET}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-http://saarthix.com:8090/profiling-api/login/oauth2/code/google}
      LOG_PATH: ${LOG_PATH:-/app/logs}
    volumes:
      - backend_uploads:/app/uploads
      - backend_logs:/app/logs
    depends_on:
      - mongodb
    networks:
      - profiling-network
      - saarthix-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 9090 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 90s

  # Frontend Service (React + Nginx)
  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:9091}
    image: profiling_frontend
    container_name: profiling-frontend
    ports:
      - "4001:4000"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - profiling-network
      - saarthix-network
    restart: unless-stopped

volumes:
  mongodb_data:
  backend_uploads:
  backend_logs:

networks:
  profiling-network:
    driver: bridge
  saarthix-network:
    external: true
    name: saarthix-network
